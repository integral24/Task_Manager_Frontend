name: Branch_tests

on:
  push:
    branches: '**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm install
      - run: npm run test
